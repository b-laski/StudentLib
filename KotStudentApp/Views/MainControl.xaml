<UserControl x:Class="KotStudentApp.Views.MainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KotStudentApp.Views.MainControlWindows"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Panel.ZIndex="2">
            <local:ToolBar x:Name="ToolBar" OpenCloseMenu="ToolBar_OpenCloseMenu" OpenProfilControl="ToolBar_OpenProfilControl"/>
        </Grid>
        <Grid Grid.Row="1" Panel.ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <local:Menu x:Name="grdMenu"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
