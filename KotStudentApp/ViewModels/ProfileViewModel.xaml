<UserControl x:Class="KotStudentApp.ViewModels.ProfileViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KotStudentApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Image Source="{Binding Cover}" />
        </Grid>
        <Grid SnapsToDevicePixels="True" Grid.Row="0" Grid.RowSpan="2" Height="60" Width="60" Effect="{StaticResource MaterialDesignShadowDepth1}" HorizontalAlignment="Left" Margin="34,0">
            <Rectangle Fill="{DynamicResource PrimaryHueMidBrush}" />
            <Image Source="{Binding Photo}" />
        </Grid>
        <Grid Grid.Row="1">
        </Grid>
    </Grid>
</UserControl>
